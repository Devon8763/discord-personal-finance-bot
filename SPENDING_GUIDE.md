# 生活支出與自然語言查詢（0.10.1）

私訊 Bot 輸入 `!help` →「生活記帳」開啟私人生活看板，按「＋記一筆消費」填表。`!記帳說明` 與帳務文字指令仍保留，所有財務與資料操作限Bot私訊，伺服器拒絕處理。0.6.3已移除 `/生活` 與 `/記帳`。

生活功能入口：

- **智慧推薦**：「更多 → 常用捷徑管理 → 查看推薦」，僅在首頁有三個啟用捷徑時，比較近30天有效手動消費的分類／啟用來源／用途組合。候選至少達3次且達首頁最低次數的2倍才顯示；卡片列出日期範圍與雙方次數。採用只開修改表單，金額與排序保留，確認後才更新，不自動取代、不主動通知。
- **常用捷徑**：新增／修改直接開啟同一張表單，填名稱、用途與選填金額，分類和付款來源在表單內下拉選取；管理模式可上下排序、停用，清單可翻頁。使用捷徑直接開啟預填確認表單，日期為今天，確認後才新增消費，不改原捷徑。現金／未指定可直接選，停用來源不可選；儲存時再次驗證。
- **最近再記**：列出依消費日期／編號倒序的6筆有效手動消費，排除固定項目、已撤銷及他人資料。選取後在同一張表單預填今天與原內容，確認才新增。來源停用時必須從表單下拉選擇自己的啟用來源（例如現金／未指定），儲存時再次驗證。
- **回顧**：週回顧比較週一至今天與上週相同星期範圍；月回顧顯示截至今天總額、主要分類、來源分布及預算，差額採上月同日範圍。短月只比較共同天數並標示兩個範圍。沒有資料不推斷零消費或差額，無消費的預算只列額度；只依已記錄資料統計，不呼叫AI或新增推播。

`!help` 主頁只保留生活與投資入口；投資AI摘要在投資紀錄的AI分頁。生活AI移至「更多 → 洞察 → 生活 AI」。0.9.5新增獨立AI同意狀態表、預設關閉，停止舊程序並備份後啟動新版，再重新開啟私訊面板；詳見 [README](README.md) 與 [更新紀錄](CHANGELOG.md)。

新增支出直接開啟單張表單，包含日期（預填今天，可輸入 YYYY-MM-DD）、金額、分類下拉、付款來源下拉、用途。設定預算同一張表單包含月份下拉、分類或總額下拉、金額。新增固定項目同一張表單包含名稱、金額、分類下拉、種類與開始月份合併下拉、期數（分期必填；固定／訂閱留空）。一般情況一次填完儲存，不需逐步選擇。

每張表單最多5欄，下拉最多25項。缺少分類／付款來源時，在下拉選「＋新增分類／＋新增付款來源」，填妥必填欄位後送出會保留草稿、不記帳。按私人新增按鈕填名稱，再按「返回原表單」，新項目自動選好，原金額、用途與日期保留。只有原消費表單確認儲存才記帳。需要更多既有選項時選「其他選項」補選。月份亦提供「其他月份」補選。日期保留文字欄位，方便在同一張表單補登任意日期。

「更多 → 設定 → 付款來源」可新增、改名、停用自己的來源；保留項目「現金／未指定」不可改名或停用。歷史消費保留當筆名稱，舊記錄一律顯示「未指定」。原文字記帳不需更改；文字修改保留來源，要變更來源請用帳目管理。

「帳目管理」按月顯示自己的有效消費，每頁6筆列出日期、金額、分類、完整用途及來源；選取單筆後才開表單修改五欄，避免直接編輯錯列。付款來源與分類下拉可保留原值（包含已停用項目），或選自己的啟用項目。自動固定支出不可移動日期；修改後可私訊 `!記帳撤銷` 預覽並確認還原。另一表單已修改同筆資料時，舊表單會拒絕覆蓋。

「支出圖表」提供看板所選月份（預設本月）的分類分布、付款來源分布及截至該月的六個月趨勢，使用原生長條、金額與占比；歷史月計整月，本月截至今天。來源依當筆保存名稱分組，同一來源改名前後可能分組顯示。只計有效消費，無資料有提示，不推算未記錄支出，亦不表示銀行或信用卡餘額。

預算頁的「提醒設定」提供常用組合、百分比多選及自訂欄位，預設80%／100%。看板「切換月份」改為下拉選單；一般月份涵蓋最近36個月及已有紀錄月份，預算另提供未來12個月。

## 新版分頁看板

`!help` → 生活記帳，直接進入生活看板；或 `!記帳說明` → 開啟生活看板。

- **今天**：本月已支出、剩餘總預算；未設定總預算顯示「尚未設定」。按「＋記一筆消費」直接開原單張表單。最多3個啟用捷徑依既有排序顯示，點選只開確認表單；無捷徑時顯示單一建立入口。
- **帳目**：預設月曆，日期格不顯示金額。`—` 無已記錄消費，`░／▒／▓` 依當月最高每日金額的1/3、2/3分界；未記錄不代表沒有消費。以前半月1～15日／後半月16日～月底選擇完整日期與星期，選日後才看總額、每頁6筆完整帳目及修改入口。搜尋、清單檢視及最近再記從「帳目工具」進入；切月保留，未來月份不可選。
- **更多**：只有「設定／洞察／常用捷徑管理」三個主要入口。「設定」下拉包含預算、固定負擔、付款來源、分類、提醒；「洞察」下拉包含支出圖表、本週回顧、本月回顧、生活 AI；捷徑管理仍可新增、修改、排序、停用及查看完整清單。
- 「更多 → 設定 → 預算」保留進度條、每頁5個預算與設定表單；「更多 → 設定 → 固定負擔」保留每頁5個設定與新增表單；「更多 → 洞察 → 生活 AI」保留月分析、週分析與問問題。
- 帳目及更多的月份選擇影響歷史清單、圖表及設定摘要；今天頁固定為實際本月。原生活 AI 分析仍以今天為基準，指定月份請用問問題。
- 看板閒置5分鐘後請重新開啟。各人只能操作自己的看板；舊文字指令繼續使用。「更多 → 設定 → 分類」可開啟既有詳細說明；「重新整理」維持原更新行為。

預算卡片使用綠／黃／紅區分未達80%、達80%、達100%；提醒通知仍依你自訂的門檻。超支時進度條顯示滿格，百分比仍顯示實際使用率，不截成100%。

## 自訂分類與提醒

- `!分類清單`：查看啟用及停用分類。
- `!分類新增 寵物`：新增自己的分類，或恢復同名停用分類。
- `!分類刪除 寵物`：停用，禁止新支出選用；歷史帳目、原預算及已設定的固定支出保留。若固定支出也要停止，另用 !固定停用。
- `!提醒設定 50 80 100`：提醒設為50%／80%／100%，也接受50%格式；適用自己的所有總預算與分類預算。
- `!提醒設定`：查看設定；`!提醒重設`：恢復預設80%／100%。可設1～10個整數門檻，範圍1～1000%。
- 修改門檻立即檢查本月；移除未送出的舊門檻通知，已送達的同月同分類同門檻不重發。
- 報表明列「使用率 XX%」；分類占總支出的比例另標「占比」，避免混淆。

各使用者的分類與門檻獨立。AI分類只建議啟用分類；歷史查詢仍可查停用分類。!生活清除會清掉自訂分類與門檻，恢復預設。

## 更簡單的新增方式

`!help` →「生活記帳」→「更多」→「設定」→「固定負擔」→「＋新增固定負擔」→ 直接填表單。亦可從 `!記帳說明` 開啟生活看板。

- **項目名稱**：例如影音平台。
- **每月金額**：例如390元；分期則填每期金額。
- **分類**：下拉選擇，例如娛樂。
- **種類與開始月份**：合併下拉選擇固定／訂閱／分期及本月／下月，例如「訂閱 · 2026-09」。
- **總期數**：分期必填，例如10；訂閱／固定支出留空。

原文字指令保留，訂閱／固定支出可省略期數：`!固定新增 訂閱 影音 390 娛樂 2026-09`。舊寫法中的0代表每月持續直到停用，不是金額。付款日仍可透過原文字指令選填。

AI 分析最多三個短重點，以空行分段；單月金額／分類查詢由程式直接呈現，不再附上重複長篇分析。統一提示為「ℹ️ 僅依已記錄資料統計。」金額整數不顯示.00，有小數則保留必要位數。

生活記帳與投資資料完全獨立。僅使用台幣，不記收入或銀行餘額。月報的「剩餘」指預算剩餘，固定支出「自動記帳」不代表銀行實際付款。

## 升級啟動

先在執行中的終端按 Ctrl+C 停止所有舊版程序，關閉連線後備份 `data.db`，使用有效Python環境執行 `python bot.py`。首次啟動自動新增付款來源資料表、消費來源快照／類型／修改版本欄位及索引，保留原生活與投資資料；舊消費來源預設「未指定」。重新傳送 `!help`，點「💰 生活記帳」進入生活選單，也可直接 `!記帳說明`。生活面板閒置5分鐘後需重新開啟，結果僅本人可見；所有財務與資料操作限Bot私訊，伺服器拒絕處理。升級與驗證結果詳見 [README](README.md) 與 [更新紀錄](CHANGELOG.md)。

## 最小測試

下例為2026年9月；固定項目的開始月必須是實際本月或下月，測試其他月份時請替換。一次貼上多行可依序執行，勿重貼已成功的支出或固定新增指令，以免重複建立。

從0.5.1起，私訊中連續生活寫入指令集中回覆各筆結果，避免每筆都等待網路；仍逐筆獨立寫入，錯誤項目會顯示序號，後續項目繼續。預算提醒在結果後送出，回覆失敗時請先查明細確認成功項目，不要整批重貼。單則仍限50行；投資／其他指令依原順序執行，不併入生活回覆。

```text
!預算 2026-09 總額 20000
!預算 2026-09 餐飲 6000
!支出 150 餐飲 午餐
!支出 40 交通 捷運
!固定新增 固定 房租 8000 居住 2026-09 0 5
!固定新增 訂閱 影音 390 娛樂 2026-09 0
!固定新增 分期 筆電 3000 購物 2026-09 10
!月報
!固定清單
```

在沒有其他生活資料時，本月合計11,580元，其中固定負擔11,390元，總預算剩餘8,420元。

## 指令

| 指令 | 說明 |
|---|---|
| `!支出 金額 分類 用途` | 記錄今天的支出，用途可含空白 |
| `!支出補登 YYYY-MM-DD 金額 分類 用途` | 補記歷史支出，不接受未來日期 |
| `!支出明細 [YYYY-MM] [頁碼]` | 每頁20筆，包含撤銷標記 |
| `!支出修改 編號 YYYY-MM-DD 金額 分類 用途` | 修改自己的單筆支出；自動項目不可移動日期 |
| `!記帳撤銷` | 預覽最近生活操作，再輸入 `!記帳撤銷 操作編號` 確認；可依序還原 |
| `!預算 YYYY-MM 總額 金額` | 設定該月總預算 |
| `!預算 YYYY-MM 分類 金額` | 設分類預算，合計不可超過總額 |
| `!月報 [YYYY-MM]` | 月支出、分類占比、固定負擔、預算使用率與剩餘 |
| `!固定新增 種類 名稱 每月金額 分類 開始月份 期數 [付款日]` | 種類為固定／訂閱／分期；前兩種期數填0，分期填1～600 |
| `!固定清單` | 本月自動記帳明細、合計、設定項目與剩餘分期 |
| `!固定停用 編號` | 停止未來自動產生，不刪除已入帳支出 |
| `!分類建議 用途` | AI 建議分類，不寫入資料；可依建議自行記帳或改分類 |
| `!記帳分析 月 3` | 最近3月同天數比較與預算建議，支援2～12期 |
| `!記帳分析 週 4` | 最近4週同星期範圍比較，週一為起點 |
| `!問 問題` 或 `!ask 問題` | 唯讀自然語言查詢 |
| `!生活清除 yes` | 清除生活資料、預算、固定設定與提醒，保留投資資料 |

分類：餐飲、交通、購物、居住、娛樂、醫療、其他。名稱有空格需加雙引號。金額最多兩位小數。

## 月初自動記帳與預算提醒

- 固定／訂閱持續到停用；分期每月記一期，期數結束不再產生。金額是每月／每期金額，不是商品總價。
- 本月開始會立即補記在本月1日，下月開始則等待下月。付款日為選填備註，不控制記帳日；短月的付款日以月底理解。
- 執行中每分鐘檢查一次；未開機期間，下次啟動補記所有應記月份，私訊通知。重啟不會再次建立同項目同月份。
- 撤銷自動產生的某月支出，不會被背景作業再次補回；不影響之後月份。單筆修改不會更改固定設定，需調整未來月額時停用後另建下月開始的項目。
- 預算按月獨立設定，不自動沿用。分類預算須先有該月總預算。
- 80%與100%門檻各提醒一次，包含固定支出；修改預算／支出後重新檢查，但已發過的門檻不重發。同時跨越兩門檻會收到兩個提示。
- 自動記帳與背景門檻提醒透過Discord私訊；若私訊關閉，保留通知至下次生活指令顯示。網路恰好在傳送成功但確認前中斷時，通知可能重送，帳目本身仍不重複。

## AI 能查什麼

```text
!問 這個月餐飲花多少？
!問 上個月花了多少？
!問 本月預算還剩多少？
!問 最近四週哪個分類占比提高？
!問 本月有哪些分期和訂閱？
!問 我有哪些持股？
!問 最近有哪些投資交易？
```

模型先產生限定的查詢計畫，由Python驗證及查詢目前使用者資料，再生成回答。模型無法指定其他使用者、執行SQL或寫入資料。支援單月生活報表、近2～12週／月比較、本月固定項目、目前持倉、最近20筆投資交易。不支援自然語言新增修改刪除、任意日期區間、歷史投資快照、新聞及銀行餘額。

月／週比較都對齊天數。若短月限制比較範圍，所有月份採相同最短天數；各期起訖日包含在提供给AI的資料中。占比變動以「百分點」計算，金額變動另列，且分類內分出固定與日常支出。月初固定記帳會造成週報集中，不代表消費突增。

没有紀錄不當作實際零消費；有紀錄也無法證明完整，AI會註明僅依已記錄資料。預算建議供參考，不會自動修改。Ollama不可用時，支出、月報、固定負擔與預算仍可使用。

## 測試

`python -m unittest discover -s tests` 使用隔離資料庫。已安裝discord.py時會包含真正指令解析的離線整合測試；不登入Discord、不讀Token。`python tests/ollama_spending_smoke.py` 是手動本地模型測試，只送假問題，不讀取個人帳目。

帳目工具、設定與洞察選單均提供「返回主看板」。子功能另開私人訊息時，可回原選單按返回；預算、固定負擔及生活 AI 頁可直接按「更多」。

## 換Discord帳號搬移資料

舊帳號在「更多 → 設定 → 我的資料與隱私 → 匯出我的資料」保存格式2 ZIP；新帳號由「匯入我的備份」上傳、預覽各類計數並確認。包含消費、分類、付款來源、捷徑、有效預算，以及有資料時的投資紀錄。重複略過、衝突保留新帳號設定；取消不寫入。AI資料／偏好不搬移，匯入後AI關閉，主動使用前重新詢問同意。詳細範圍、預算與投資衝突規則見 [README](README.md) 與 [隱私說明](PRIVACY.md)。

無法登入舊帳號且沒有相容備份即無法找回；Bot不提供管理者查看、搜尋、代轉移或還原權限。主機管理者仍可能直接讀庫。所有操作僅Bot私訊，格式與完整性檢查不是來源認證。

0.10.1匯出入口統一為「匯出我的資料」；送出前檢查ZIP可被現行格式2匯入流程接受。只檢查格式與完整性，不保證來源真偽；沒有新增簽章。若超出8 MiB、解壓32 MiB或每種資料10000筆限制，匯出失敗時不傳送附件、不截斷資料。
