# 本機備份與整體復原（0.10.0）

**整體復原不是取消刪除，也不是個別使用者還原。** 所有維護命令在主機的專案目錄執行，Discord沒有還原按鈕或指令。請不要把資料庫或主機備份傳到Discord、GitHub、雲端同步或公開目錄。

## 位置與保留期

- 沿用 `db.DB_NAME` 資料庫路徑；同層 `backups/` 保存完整SQLite備份。使用SQLite線上備份API，不直接複製運作中的資料庫。
- 同層 `.safety/state.sqlite3` 保存備份檔名／用途／快照時間／SHA-256、刪除標記、健康狀態與待送事件；不保存帳目內容。刪除標記只有Discord使用者ID與刪除時間。這份狀態不可與舊帳本一起還原或自行刪除，否則備份會失去驗證依據。
- 正式帳本持續保存。只有主機備份與刪除標記保留7天；使用者自行下載的匯出ZIP無法由Bot遠端清除。
- Bot啟動時補做當日備份，連線後每分鐘檢查。日常備份以台灣日期去重，重啟不重複；更新前及緊急備份可另外建立。完整性或校驗失敗不算成功。
- 超過保留期的登錄備份及符合本模組命名的中斷殘檔會清理；先清理備份，再淘汰刪除標記。清理失敗時保留標記，避免舊資料復活。停機或磁碟權限故障期間無法實際刪檔，會在下次可執行時清理；即使檔案還在，也拒絕復原滿7天的備份。
- `.gitignore` 排除 `backups/`、`.safety/` 與復原候選檔。拒絕UNC、符號連結／接合點與常見同步／Public路徑；無法偵測所有第三方同步軟體，主機管理者仍須確認整個專案不在同步位置，並設定本機檔案權限。Windows ACL、磁碟加密及實體抹除未自動設定。

## 唯一管理者通知

在啟動Bot的同一個PowerShell視窗設定環境變數：

```powershell
$env:ADMIN_DISCORD_ID = Read-Host '請輸入唯一管理者的 Discord 使用者 ID'
python bot.py
```

只接受一個正整數ID，不支援多管理者。`.env.example` 是說明範例，不會自動載入 `.env`。不要把真實ID填進原始碼、README或提交到Git。

未設定時仍執行本機備份並保存異常，但不能私訊；錯誤格式則拒絕啟動。管理者需能接收Bot私訊。通知只有技術分類（database／daily／pre-update等）、UTC時間、異常／恢復狀態及處理建議，沒有帳目、使用者ID、金額、訊息或AI內容。

同一持續問題只登錄一次異常，恢復再登錄一次；Discord無法連線或私訊失敗時保留待送事件，下次成功連線補送。若整個維護狀態目錄也不可寫入，無法保證保存待送事件，會顯示安全錯誤並需人工處理。Discord已收件但主機在記錄送達前崩潰的極端情況，補送可能重複；不宣稱跨網路絕對一次送達。

## 每次更新前必做

先停止Bot，再執行：

```text
python service_safety.py pre-update
```

看到「更新前備份完成」及新檔名才繼續更新程式；非零結束碼或失敗提示時先停止更新、排除問題。此命令可線上備份，但正式更新仍建議先停Bot。第一次導入本功能，先取得本版 `service_safety.py`，對既有資料庫執行此命令，再部署其他更新。**開發測試不會替你備份正式資料庫。**

更新前失敗會保存異常，下一次Bot連線後通知；重新執行成功才解除該項異常。此入口是明確維護步驟，不猜測檔案改動，也不自動更新程式。

## 查看狀態與可選備份

```text
python service_safety.py status
```

僅顯示最近成功備份時間、最近健康檢查狀態及備份檔名／種類／快照時間，不顯示任何人的帳目。健康狀態是上次檢查結果，需注意時間，並不表示離線主機剛完成新檢查。

## 整體系統故障時復原

1. 停止所有Bot版本與資料庫工具。新版Bot會持有本機維護鎖，運行時復原會拒絕；舊版或外部工具不一定遵守此鎖，仍須人工確認。
2. 用 `status` 選擇7天內、時間正確的本機維護備份。不能使用使用者匯出的ZIP、任意SQLite檔案或失去登錄的備份。
3. 確認接受**全體使用者的生活及投資資料一起退回該備份時間點**；快照之後的新增／修改不會自動合併。不是讓管理者查看、挑選或復原某人的帳目。
4. 把下列佔位檔名換成狀態列出的完整備份檔名，再明確確認：

```text
python service_safety.py restore --backup "備份檔名.sqlite3" --confirm-whole-database
```

程式驗證登錄、SHA-256、期限及SQLite完整性，先建立目前資料庫的緊急備份，再於候選資料庫套用刪除標記，驗證完整性與可寫入後才替換。缺少確認旗標不會復原。緊急備份失敗、候選損毀、讀寫驗證或替換失敗時會停止；不得因急著恢復而直接手動覆蓋資料庫。

**若目前資料庫已嚴重損毀，無法建立通過驗證的緊急備份，本指令也會拒絕覆蓋。** 此版沒有損毀頁面修復、略過保護或強制復原選項，需另行規劃主機故障救援。硬碟整體損毀時，同機備份也可能一併遺失；本輪沒有異機或雲端備援。

看到成功提示後，重新啟動Bot並查看服務狀態。原始備份不會因套用刪除標記被修改。

## 刪除資料不會因復原復活

原「刪除我的生活資料」與 `!生活清除 yes` 的操作不變，只清生活資料，投資保留。生活資料刪除與外部刪除標記使用同一SQLite附加資料庫交易提交，任一失敗一起回滾。維持既有DELETE journal模式，不支援自行改為WAL後繼續此刪除流程。

| 備份時間 | 復原處理 |
| --- | --- |
| 早於或等於該使用者最近刪除時間 | 清除候選資料庫中該使用者的生活資料，舊帳不復活；投資不因生活刪除被清除 |
| 晚於刪除時間 | 保留備份內刪除後重新建立的生活資料 |

例如週二刪除、週三重新記午餐：還原週一備份時舊帳清除，午餐不在該備份所以無法找回；還原週三刪除後備份時保留午餐。這不是從故障中的現有資料庫抽取新帳合併。請保持主機時鐘準確，時間判斷依UTC快照與刪除標記。

應用程式不提供管理者個別帳本查看／還原入口，但具有主機檔案權限的人仍可能直接讀取SQLite；本功能不宣稱主機管理者無法讀取資料或資料已加密。

## 驗證限制

自動測試僅使用隔離資料庫、暫存備份及Discord替身；沒有讀取Token、操作正式資料或傳送真實管理者私訊。尚未實測長時間跨日運行、真實磁碟故障／斷電、管理者私訊權限、主機ACL、第三方同步偵測及外部資料庫工具的併發操作。沒有新增網站、App、投資／AI功能、帳目匯入或記帳介面。
