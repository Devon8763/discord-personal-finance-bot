# 專案更新紀錄規則

- 每次完成專案修改，必須同步更新根目錄 `CHANGELOG.md`，以繁體中文記錄本次實際新增、修改、修正與移除的內容。
- 紀錄包含版本、日期、受影響指令、資料庫變更、必要啟動／升級步驟，以及實際驗證結果與未驗證事項。沒有變動的分類可省略。
- 最新版本置頂；保留歷史版本，不把計畫中或尚未完成的功能寫成已完成。
- 版本從 `0.1.0` 起算。大型功能或整體流程更新才遞增次版本（例如 `0.2.0` → `0.3.0`）；小型新增、介面微調及修錯遞增修訂版本（例如 `0.2.0` → `0.2.1`）。同一批尚未交付的修改合併成一筆紀錄。
- 單純補充歷史紀錄或文件、不改變程式行為時，不必新增功能版本；在目前版本的文件紀錄註明。若使用者指定版本，依使用者指示。
- README 的目前版本應與 CHANGELOG 最新版本一致。最終回覆簡述版本與主要變更，並附更新紀錄連結。
- 不記錄 Token、憑證或個人投資資料。測試不可更動正式 `data.db`，使用隔離的測試資料庫。
- 每次工作先拆成小任務，依序實作與驗證，避免一次展開過多工作；同一輪修改仍合併交付與更新紀錄。
