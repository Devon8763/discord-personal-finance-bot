# 更新紀錄

從本次整理的第一批程式更新起，以 `0.1.0`（0.1 版）開始計算；更早的原始專案未編列版本。最新版本在上方。

依使用者於2026-09-10調整的規則：大型更新遞增 `0.X.0`，小型新增、介面調整與修錯遞增 `0.X.Y`。歷史版本重新編號，原編號保留於標題供對照；歷史功能與驗證紀錄不變。

## 0.9.5 — 2026-09-12

### 文件與 Git 安全設定補充

- 補強根目錄 `.gitignore`，保留既有虛擬環境、驗證套件、快取、Token及SQLite旁檔規則；新增環境祕密、金鑰、SQLite檔案與旁檔、備份／匯出／上傳／日誌目錄、Bot匯出ZIP、Python及工具快取、編輯器與系統暫存規則。明確保留 `.env.example`，不忽略原始碼、測試、需求及說明文件。
- 僅文件／Git設定調整，版本維持0.9.5，無程式行為、指令、資料庫或套件變更，無須重啟Bot或資料庫升級。上傳前需自行檢查暫存清單與範例設定內容；已追蹤檔案不會因新增忽略規則自動解除追蹤。
- 使用獨立暫存Git儲存庫及虛構路徑驗證：33個敏感／產物路徑被忽略、9個原始碼／測試／文件／範例路徑保留，共42項通過。本次未重跑程式回歸測試，以下140項為功能實作時的驗證紀錄。
- 目前專案目錄不是Git儲存庫，無法核對GitHub或其他儲存庫追蹤狀態；只確認本機存在 `token.txt`、`data.db` 檔名，未讀取或修改內容。未執行上傳、加入追蹤或解除追蹤；任意命名且位於忽略目錄外的備份、匯出與真實設定仍需使用者手動確認。

### 自行換Discord帳號搬移資料

- 延用「更多 → 設定 → 我的資料與隱私」匯出／匯入，格式2 ZIP新增設定JSON，白名單欄位、版本及SHA-256完整性驗證。包含消費、分類、付款來源、常用捷徑、本月及未來最新預算；本人有投資資料才附持倉、基金交易、觀察清單、基金淨值及必要歷史／撤銷關聯。不輸出舊Discord ID、AI資料／偏好、Token、管理者設定、他人資料或完整資料庫。
- 預覽逐類顯示備份、略過、預計新增／套用數；本人明確確認後立即交易重新去重。消費按八個實際內容欄位比對，同名設定／捷徑保留目標，目標已有預算則略過備份預算；同投資標的已有資料整組略過，新標的保留撤銷關聯。取消不寫入，失敗全部回滾並安全回報計數。
- 財務文字指令及共用表單／看板限定Bot私訊，伺服器拒絕處理；原非財務說明入口保留。生活／投資AI新增首次使用同意門檻，預設關閉、同意後重新開功能才執行；成功匯入清除目標AI同意，取消或失敗不改偏好。
- 未加入每日備份、刪除帳號、管理者告警、簽章金鑰、多幣別、App帳號或外部服務。完整性不保證來源真偽；管理者無應用程式代辦權限，主機仍可能直接讀取本機資料庫。README、PRIVACY及使用說明同步記錄無舊帳號／無相容備份不可找回限制。

### 升級與驗證

- 停止舊Bot、關閉連線並自行備份資料庫後啟動新版，重新私訊開面板。新增獨立 `ai_preferences(user_id, enabled)` 表，預設關閉、升級可重複執行；既有財務資料表結構與資料保留，無新增套件。格式1及更舊備份需由舊帳號重新匯出格式2。
- 先寫失敗測試再實作；完整 `tests/run_discord_validation.py` 140項隔離測試通過，涵蓋往返、設定／投資衝突、重複匯入、撤銷關聯、預覽取消、格式／完整性拒絕、本人隔離、伺服器拒絕、AI同意及不搬移、整批回滾與安全失敗計數，既有生活／投資回歸通過。
- 未讀取Token、未啟動正式Bot、未讀寫正式 `data.db` 或真實備份。Discord桌面／手機附件、跨帳號預覽／確認與AI同意卡未實機驗證；正式升級、真實Ollama、主機檔案權限、磁碟加密及備份保護未實測。請使用者重啟Bot後實際驗收。

## 0.9.4 — 2026-09-12

### 使用者自行換Discord帳號搬移帳目

- 在「我的資料與隱私」新增「匯入我的備份」，使用既有Discord套件的FileUpload私人表單；舊帳號自行匯出、新帳號自行上傳、預覽與確認。匯出／匯入／刪除操作限定Bot私訊，伺服器先拒絕、不讀取附件。目標固定為目前操作的Discord使用者，不採用舊ID或提供管理者代辦入口。
- 既有CSV ZIP新增 `manifest.json` 格式版本1及SHA-256完整性值、`consumptions.json` 原始消費資料；CSV公式保護不影響匯入原文。依使用者確認，只檢查格式、版本與完整性，不新增簽章金鑰、不保證來源真偽。舊版缺manifest、任意CSV／ZIP、損毀、缺檔、額外檔案、欄位或版本錯誤拒絕；上限8 MiB／解壓32 MiB／10000筆，記憶體處理，不解壓落地。
- 只搬移消費，包含撤銷狀態與已入帳固定／訂閱／分期消費。保留當筆分類、來源名稱及來源類型作歷史資料，不建立舊分類／付款方式設定或固定規則；不搬移捷徑、預算、投資、AI對話等資料，舊帳號資料不刪除。
- 依日期、金額、用途、分類、付款來源名稱、來源類型、撤銷狀態與消費種類完全相同比對；忽略流水號、舊ID、關聯ID、修改版本與匯出時間。預覽顯示總數、略過與預計新增，確認時立即交易重新去重；同檔再匯入及備份內完全重複列均略過。取消／逾時不寫入；失敗整批回滾並安全回報計數，不輸出附件／資料庫例外細節。
- README／PRIVACY說明換帳號流程、無法登入舊帳號且無相容備份即無法找回，以及應用程式無管理者代辦權限與主機仍可直接讀庫的界線。

### 升級與驗證

- 從0.9.3升級：停止舊Bot並重新啟動，重新開啟私訊面板；無新增資料表、欄位、索引或套件，不修改Token或既有備份。舊帳號須重新匯出新版ZIP才能使用匯入。未新增多幣別、管理者轉移、App帳號系統、AI、爬蟲、銀行串接或外部服務。
- 先寫失敗測試後實作；完整 `tests/run_discord_validation.py` 130項隔離測試通過，涵蓋往返、去重／重複匯入、取消／逾時、當前使用者、DM限制、格式與完整性錯誤、限制、固定／撤銷資料、交易回滾／安全計數與匯入後修改。既有生活與投資回歸通過。
- 未讀取Token、未啟動正式Bot、未讀寫正式 `data.db` 或真實備份。Discord桌面／手機FileUpload附件與跨帳號實際確認畫面未實機驗證；主機檔案權限、磁碟加密、備份保護亦未實測。完整性校驗非來源認證。

## 0.9.3 — 2026-09-12

### 月曆星期改為 ASCII 英文縮寫

- 因 Discord 中文字型欄寬不穩，生活帳目月曆星期改為 `Mon Tue Wed Thu Fri Sat Sun`。星期與日期共用三字元欄位及同一個列產生函式，明確以一個半形空白分隔；空白日期固定三個空白，各欄起點為0、4、8、12、16、20、24。移除中文全形寬度補白邏輯。
- 僅改月曆標題列及排列，日期選單仍使用中文星期；深淺標記、資料統計、空月份、選日明細、前後半月、月份切換、搜尋與帳目修改不變。
- 影響生活看板帳目頁及私人月曆頁；無資料庫、套件或投資功能變更。從0.9.2升級：停止舊Bot、重新啟動後開啟新版面板，無需資料庫遷移。
- 先更新測試確認失敗，再做最小修正；完整 `tests/run_discord_validation.py` 121項隔離測試通過，涵蓋星期／日期起點、2026-09第一週、星期一／星期日起始月、平年及閏年二月與既有生活／投資回歸。
- 未讀取Token、未啟動正式Bot、未讀寫正式 `data.db`。請使用者重啟Bot後查看Discord桌面與手機實機畫面；本輪未驗證實際字型渲染，不將字串測試視為實機驗收。

## 0.9.2 — 2026-09-11

### Discord 月曆欄位對齊修正

- 已閱讀使用者提供的實機截圖 `月曆.png`，針對 `calendar_embed()` 的星期／日期混排錯位修正。新增最小共用 `calendar_cell()`：中文全形占兩格，半形日期與深淺標記占一格，每欄固定四格，星期置中且日期採同一左側起點；空白日期占滿四格。星期與日期欄直接串接，不再由 join 額外插入欄間空格。
- 保留「一 二 三 四 五 六 日」、日期／深淺標記與不顯示金額的設計；未改深淺統計、月曆選日、前半月／後半月、月份切換、帳目修改、搜尋或其他功能，未改為英文星期。
- 影響生活看板帳目頁及月曆私人頁；無資料庫、套件、外部服務或資料規則變更。從0.9.1升級只需停止舊Bot、重新啟動並開啟新版生活面板，不需資料庫遷移。
- 先新增／調整失敗測試再實作。完整 `tests/run_discord_validation.py` 121項隔離測試通過，使用中文兩格／半形一格的顯示寬度模型逐欄驗證，涵蓋2026-09星期一留白與星期二01／星期日06、星期一與星期日起始月、平年及閏年二月、四種深淺標記；既有生活、投資及月曆互動回歸通過。
- 未讀取Token、未啟動正式Bot、未讀寫正式 `data.db`。已依截圖修正固定欄位策略，但尚未取得新版Discord桌面／手機實機畫面；使用者仍需重新啟動Bot後實際查看驗收，不以字串測試宣稱各平台字型渲染已驗證。

## 0.9.1 — 2026-09-11

### 我的資料與隱私

- 新增「更多 → 設定 → 我的資料與隱私」私人頁，說明生活資料範圍、本機儲存與管理者可見性、Discord私人回覆不是端對端加密；今天與帳目首頁不增加按鈕。
- 匯出先顯示敏感資料提醒，再以私人ZIP附件提供本人生活消費、預算、來源、分類、捷徑、固定規則、提醒設定、導覽狀態與生活使用紀錄。CSV為UTF-8 with BOM、金額以元保留兩位小數，附README；空資料仍產生有效ZIP。使用固定欄位白名單與一致的只讀快照，不匯出投資、完整資料庫、系統／AI設定或秘密；記憶體產生，成功或失敗後關閉，不在專案留下使用者檔案。文字公式開頭加單引號保護。
- 新增私人刪除確認卡。「匯出後再刪除」只匯出，另按「確認永久刪除」才執行；取消、關閉、逾時、跨使用者均不執行刪除。成功／失敗更新原確認卡並移除按鈕，成功後刷新生活看板。
- UI及 `!生活清除 yes` 共用 `spending.clear` 交易，保留投資及其他使用者；0.9.1起也清除導覽狀態，避免完整清除後仍保存生活使用者偏好。生活提醒、撤銷等內部紀錄亦清除。匯出不含內部撤銷快照／提醒佇列，不新增還原功能。
- 新增 `PRIVACY.md` 並更新README路徑，說明本地AI、匯出檔保護及資料庫刪除不代表安全覆寫磁碟或撤回Discord／備份副本。錯誤訊息不輸出完整例外或檔案位置，本功能不記錄匯出內容。

### 升級與驗證

- 影響生活面板及 `!生活清除 yes` 的完整清除範圍；無新增資料表、欄位、索引、套件、銀行串接、雲端同步、外部分析、背景工作或推播。投資與爬蟲不變。
- 從0.9.0升級：停止舊Bot後重新 `python bot.py`，重新開啟生活面板／私人選單；不需新遷移。從更舊版升級仍執行既有非破壞性資料庫升級，請先自行備份。
- 先新增失敗測試後實作；完整 `tests/run_discord_validation.py` 118項隔離測試通過，包括匯出白名單與中文CSV、空資料、無落地檔案、本人隔離、附件資源清理、取消／逾時／確認、共用清除範圍、刪除回滾與安全錯誤。既有生活及投資回歸通過。
- 未讀取Token、未啟動正式Bot、未讀寫正式 `data.db`。Discord桌面／手機附件下載、確認流程與按鈕移除、Excel中文呈現仍未實機驗證；實際主機檔案ACL、磁碟加密、備份權限及安全抹除限制未測試或修改。

## 0.9.0 — 2026-09-11

### 首次使用導覽與手動月結摘要

- 生活看板新增私人首次使用導覽，與可直接記帳的主看板分開提供。已有生活資料者不自動顯示；預設現金／未指定不算自訂資料。設定付款來源、建立捷徑、設定當月預算重用既有流程，可返回原導覽卡；跳過／完成永久停止自動出現。
- 「更多 → 設定 → 重新開啟新手導覽」只手動開啟，不重設自動狀態。未選跳過／完成且仍無生活資料時，下次開啟可再出現；生活清除保留導覽偏好。
- 「更多 → 洞察 → 本月結帳」以看板月份顯示月結摘要：有效支出總額、總預算、前三大分類、來源分布、未指定筆數／金額與上月比較。本月比較共同日期，歷史月份比較完整月份；明列日期、無資料不算差額、無預算不推論為零。保留既有固定項目統計口徑，名稱安全處理 Markdown，資料多時翻頁，僅本人可見。
- 影響 `!help`／`!記帳說明` 進入的生活面板；既有文字指令、投資、爬蟲與其他商業規則不變。月結純讀取，不呼叫 AI、不新增推播、排程或外部套件。

### 資料庫升級與驗證

- 新增最小獨立表 `spending_onboarding(user_id TEXT PRIMARY KEY)`，只記錄跳過／完成導覽的 Discord 使用者 ID。未修改既有資料表欄位或投資模型；建立表可重複執行。
- 從0.8.3升級：停止舊Bot並關閉連線、自行備份 `data.db`，再啟動 `python bot.py` 自動升級；重新開啟生活面板。既有生活與投資資料保留，SQLite交易及失敗回滾策略不變。
- 先新增失敗測試，再實作；完整 `tests/run_discord_validation.py` 112項隔離測試通過，包含導覽狀態、既有資料排除、設定流程與本人隔離、重複升級，以及月結日期比較／空資料／預算／來源／有效消費／Markdown／翻頁／純讀取。既有生活與投資回歸通過。
- 未讀取Token、未啟動正式Bot、未讀寫正式 `data.db`。Discord桌面／手機實機的導覽訊息順序、返回原卡、按鈕換行、月結翻頁與可讀性尚未實測；本次使用真實Discord元件搭配傳送替身驗證。

## 0.8.3 — 2026-09-11

### 生活入口整理與導覽

- 保留今天／帳目／更多；今天的單張消費表單與最多三個常用捷徑不變。帳目頁將搜尋帳目、清單檢視、最近再記合併為「帳目工具」私人下拉選單，訊息與placeholder完整列出三個功能；月曆文字提示使用帳目工具。主帳目頁移除重複帳目管理按鈕，選日、修改、記帳與切月保留。
- 更多頁只保留設定、洞察、常用捷徑管理三個主要入口。設定下拉完整列出預算、固定負擔、付款來源、分類、提醒；洞察列出支出圖表、本週回顧、本月回顧、生活 AI。移除被取代的直接按鈕、更多頁切月／整理及重複指令說明；文字指令未刪除。
- 工具選單明列功能與返回路徑，提供「返回主看板」；子功能另開私人訊息時可回原工具選單返回。預算、固定負擔及生活 AI 切換對應看板頁，按「更多」返回。付款來源、分類、提醒、搜尋、圖表、回顧、再記與捷徑管理沿用原功能及身份檢查。
- README在三頁介紹後新增「我想做什麼？」操作路徑表，並更新搜尋、完整清單、最近再記、設定、洞察等段落及生活使用說明。

### 升級與驗證

- 影響 `!help`／`!記帳說明` 進入的生活看板。無資料庫結構、索引、套件、外部服務或背景工作變更；不改投資、爬蟲或記帳規則。
- 從0.8.2升級：停止舊程序後重新 `python bot.py`，重新開啟生活面板及子選單；無需資料庫遷移。
- 先新增失敗測試再最小實作；`tests/run_discord_validation.py` 105項隔離測試通過，包含合併入口／選項／提示、返回、全部子功能可達、私人回覆及跨使用者防護。原搜尋與圖表測試透過新選單進入，既有生活／投資回歸通過。
- 未讀取Token、未啟動正式Bot、未修改正式 `data.db`。Discord以測試替身驗證；桌面／手機實機按鈕換行、選單提示與可讀性尚未實測。

## 0.8.2 — 2026-09-11

### 三項生活介面修正

- 批次生活記帳結果改為兩位數 `01｜`～`50｜` 序號，標題後空一行；每筆回覆的第二行起縮排四個半形空白，同筆指令的多次回覆歸在同一序號。維持各筆獨立執行、失敗不影響其他筆、結果發送後才處理通知，不自動重送成功項目。
- 搜尋帳目開始／結束日期接受 `YYYY-MM-DD`、`YYYY/MM/DD`、`YYYYMMDD`。共用資料層統一解析為 `YYYY-MM-DD`；表單標題提示三種格式。不存在日期、混用分隔符號、句點、缺少補零、未來日期與起日大於迄日仍拒絕；搜尋邊界與資料隔離不變。
- 帳目月曆星期標題依中文顯示寬度修正欄距，與日期格起點對齊。日期深淺標記、統計規則、選日明細與前／後半月操作不變。

### 升級與驗證

- 影響私訊批次生活指令結果、生活帳目頁的搜尋表單與月曆。無資料表、欄位、索引、套件、服務或背景工作變更；投資與爬蟲未改動。
- 從0.8.1升級：停止舊程序後重新執行 `python bot.py`，重新開啟生活面板及表單，無需資料庫遷移。
- 先加入失敗測試再做最小修改；`tests/run_discord_validation.py` 102項隔離測試通過。新增三種日期格式、無效日期格式、批次多行／多次回覆縮排及中文星期／日期格欄距測試；原逐筆失敗處理、通知與生活／投資回歸通過。
- 未讀取、修改或刪除正式 `data.db`，未讀取Token、未啟動正式Bot。Discord傳送採測試替身；桌面／手機實際字體、縮排與月曆對齊尚未實測，欄距測試以中文寬2格、ASCII與深淺符號寬1格驗證。

## 0.8.1 — 2026-09-11

### 搜尋帳目

- 帳目頁新增「搜尋帳目」，沿用單張表單，只有名稱／用途關鍵字、開始日期及結束日期三個選填欄，至少填一項。三欄空白不執行查詢，顯示友善提示。
- 新增純讀取 `search_expenses()`，用途部分文字比對與日期條件同時生效。只填開始日查至今天；只填結束日查該日及之前有效紀錄；雙日期含起訖日。格式錯誤、未來日期及起日大於迄日會拒絕。
- 只列本人 `kind='consumption'`、`voided=0`、`source='manual'` 紀錄，排除固定／訂閱／分期及其他使用者。依日期、編號由新到舊，每頁6筆，重用帳目管理格式與既有單筆修改表單；點選重新讀取紀錄，保留本人、付款來源／分類及版本衝突驗證。無結果顯示友善提示，結果與操作皆私人回覆。

### 升級與驗證

- 影響 `!help`／`!記帳說明` 進入的帳目頁。無資料表、欄位、索引、套件、外部服務或背景工作變更；投資、來源管理、捷徑、月曆及既有文字指令未改動。
- 從0.8.0升級：停止舊程序後重新執行 `python bot.py`，重新開啟生活面板；不需資料庫遷移。
- 先新增失敗測試後最小實作，`tests/run_discord_validation.py` 99項隔離測試通過。新增5項測試涵蓋部分文字與特殊字元、所有日期條件與邊界、空白／格式／未來／起訖錯誤、排除資料、純讀取、六筆分頁、私人修改與過期紀錄拒絕；原生活與投資回歸通過。
- 未讀取Token、未啟動正式Bot、未修改正式 `data.db`。Discord傳送採測試替身，桌面／手機實際搜尋與修改畫面尚未實測。

## 0.8.0 — 2026-09-11

### 30 天智慧捷徑推薦

- 新增純讀取 `shortcut_recommendation()`：首頁三個啟用捷徑存在時，統計今天往前29天至今天（含首尾）有效手動消費的「分類＋啟用付款來源ID＋用途」組合。排除首頁組合、停用分類／來源、固定／訂閱／分期、已撤銷、非消費及其他使用者資料。
- 候選至少達 `max(3, 首頁最低使用次數 × 2)` 才推薦；最低次數同分取首頁位置較後的捷徑。只在常用捷徑管理提供「查看推薦」，顯示雙方次數及日期範圍，不主動通知或建立排程。
- 「採用建議」重新確認推薦後開既有捷徑修改表單：候選分類、付款來源、用途預填，名稱取用途（沿用30字限制，較長者取前30字）；金額保留目標原值、排序不變。確認才更新指定捷徑，返回、忽略或關閉不寫入，也不新增消費。

### 帳目月曆

- 帳目頁與帳目管理預設顯示完整月份月曆，日期格只有日期與深淺。有效消費（含有效固定支出）按日加總，以當月最高日總額分為 `░ ≤1/3`、`▒ >1/3且≤2/3`、`▓ >2/3`，無紀錄為 `—`，標明「未記錄不代表沒有消費」。
- 日期選單固定前半月1～15日、後半月16日～月底，後半最多16項，顯示完整日期與星期。主帳目頁切半月保留今天／帳目／更多導覽；切月份不提供未來月。
- 選日期後才顯示當日總額、每頁6筆完整帳目及既有修改表單；清單檢視、最近再記與月份切換保留，空月份提供切月或快速記帳入口。

### 升級、相容性與驗證

- 版本0.8.0；影響 `!help`／`!記帳說明` 進入的帳目頁與常用捷徑管理。既有文字指令、付款來源、預算、固定負擔、生活AI及投資功能保留；未修改爬蟲。
- 無資料表、欄位、索引、套件、外部服務或背景排程變更。從0.7.x升級只需停止舊程序、重新 `python bot.py`，重新開啟面板，無需資料庫遷移。
- 先寫失敗測試後最小實作；`tests/run_discord_validation.py` 完整94項隔離測試通過，含近30天邊界／門檻／排除／純讀取、確認前不寫入、過期推薦、月曆分界／空月／閏年／日期分段、單日分頁修改、月份與使用者隔離及原生活／投資回歸。
- 未讀取Token、未啟動正式Bot、未修改正式 `data.db`。Discord傳送與外部功能用替身驗證；桌面／手機月曆星期對齊、深淺可讀性、日期切換及真實確認操作未實測。

## 0.7.1 — 2026-09-11

### 新增與調整

- 分類與付款來源下拉直接顯示「＋新增分類／＋新增付款來源」，適用於既有記帳、修改、捷徑與其他共用表單。原更多選項改標為「其他選項」，保留分頁補選功能。
- 選取新增、填妥必填欄位並送出後，只保留草稿；透過私人按鈕開名稱表單，新增完成按「返回原表單」會自動預選新項目並保留金額、用途、日期等欄位。新增項目不會提前新增消費，仍須確認原表單。
- 每個下拉最多25項，含新增與更多入口；超過首頁容量的原預選仍保留。新入口、名稱提交與返回均檢查操作者，儲存消費仍驗證來源啟用狀態。

### 入口、升級與驗證

- 影響 `!help`／`!記帳說明` 開啟的生活共用表單；既有生活文字指令、三頁配置及投資功能不變。無資料表、欄位、索引、套件或外部服務變更。
- 從0.7.0升級：停止舊程序後重新執行 `python bot.py`，重新開啟生活面板及表單，無需資料庫遷移。
- 先新增失敗測試，再完成最小實作；`tests/run_discord_validation.py` 85項隔離測試通過。新增3項測試覆蓋分類／來源建立、草稿保留與自動選取、確認前不記帳、跨使用者拒絕、來源停用驗證、空選單與大型清單容量；既有生活／投資回歸通過。
- 未讀取Token、未啟動正式Bot、未修改正式 `data.db`。Discord送出以替身驗證，手機／桌面實際下拉畫面與新增往返尚未登入驗證。

## 0.7.0 — 2026-09-11

### 生活面板介面精簡

- 五個同層頁籤改為「今天／帳目／更多」。今天只顯示實際本月已支出、剩餘總預算、主要操作「＋記一筆消費」及最多3個依排序取得的啟用捷徑；沒有總預算顯示「尚未設定」，沒有捷徑提供單一建立入口。
- 首頁捷徑重新讀取本人的啟用設定後，才開既有預填確認表單；不能提前記帳，已停用捷徑或非本人操作會拒絕。分類、付款來源、日期與金額沿用既有單張表單與驗證。
- 帳目頁集中月份切換、每頁6筆清單、帳目管理及最近再記。原月份清單、有效消費修改表單、撤銷與最近6筆有效手動消費規則保留。
- 更多頁集中財務設定、洞察與捷徑管理；預算、固定負擔與生活 AI 保留原有內容與操作，從更多進入。分類沿用原指令說明；圖表、週／月回顧、提醒、付款來源與完整捷徑管理均保留。更多頁16個元件，沒有重新塞回首頁。

### 入口、資料與升級

- 影響 `!help` → 生活記帳、`!記帳說明` 進入的生活看板與提示文字。原生活文字指令、投資入口及投資 AI 分頁不變；未恢復已移除的斜線入口。
- 無資料表、欄位、索引、套件或外部服務變更；消費、預算、來源、固定負擔、圖表、回顧與 AI 商業規則不變。未建立其他平台 App，未修改爬蟲。
- 從0.6.x升級：停止舊程序後重新執行 `python bot.py`，再以 `!help`／`!記帳說明` 開啟新版面板；舊的按鈕訊息與表單請重新開啟。無需手動遷移資料庫。

### 驗證

- 先新增失敗測試，再最小實作與調整原五頁籤測試；`tests/run_discord_validation.py` 完整82項測試通過，全部使用隔離資料庫。
- 覆蓋今天摘要、歷史月份不影響今天、空狀態、前三個啟用捷徑及排序、重新讀取、確認前不寫入、跨使用者拒絕、帳目與更多入口、私人回覆和View容量；既有生活／投資回歸通過。
- 未讀取Token、未啟動正式Bot、未修改正式 `data.db`。Discord桌面／手機實際畫面、按鈕可讀性及真實互動未驗證；傳送與外部功能仍以測試替身驗證。

## 0.6.3 — 2026-09-11

### 文件

- 新增生活記帳「今天／帳目／更多」介面精簡設計規格；此為待確認的後續設計，尚未變更程式行為、資料庫或版本功能。
- 新增已確認的介面精簡實作計畫；尚未開始程式實作，未改變功能、資料庫或套件。
- 新增「30 天智慧捷徑推薦與帳目月曆」設計規格；尚未開始程式實作，未改變功能、資料庫或套件。
- 新增已確認的智慧捷徑與帳目月曆實作計畫；尚未開始程式實作，未改變功能、資料庫或套件。

### 修正與移除

- 依使用者要求移除 `/生活`、`/記帳`，更新主頁提示、空狀態與使用說明；完整私人面板改由 `!help` →「生活記帳」進入，按「＋記支出」直接開啟原單張表單。`!記帳說明`、既有文字指令與投資功能保留。
- 啟動仍同步指令清單，以移除Discord端舊斜線入口；失敗僅顯示安全日誌，文字功能保留。
- `yfinance` 改為首次查價時才匯入，避免啟動預先載入pandas阻擋生活功能。原背景查價、上市／上櫃候選、快取與計算不變；沒有重裝套件，也未判定使用者環境的套件損壞。

### 升級與驗證

- 無資料庫、索引或依賴變更。停止舊程序後重新執行 `python bot.py`，以 `!help` 開啟新版面板；不需移出並重新邀請Bot。
- 77項隔離測試通過：新增行情延後匯入測試，確認啟動匯入不碰行情套件且查價仍嘗試上市／上櫃；指令清單為空、同步及私人主頁入口與既有生活／投資回歸通過。
- 未讀取Token、未碰正式 `data.db`、未啟動正式Bot。Discord同步與行情以替身驗證；Discord端舊指令移除、使用者Python 3.13套件載入耗時、實際連線啟動與即時行情未驗證。`KeyboardInterrupt`僅證明載入被中斷，本次修正啟動載入時機，不宣稱修復套件本身。

## 0.6.2 — 2026-09-11

### 修改

- 生活新增支出、帳目修改、捷徑新增／修改／使用、最近再記改為直接開啟單張表單，分類與付款來源在表單內下拉選取；金額、用途與日期同時填寫，確認儲存才記帳。新增及再記日期預設今天，日期使用 YYYY-MM-DD 文字欄位。
- 預算與月份表單內嵌下拉；固定項目直接開啟表單，分類下拉與「種類／開始月份」合併下拉，期數欄分期必填、固定／訂閱留空。
- 重用既有分類與來源管理。超過下拉容量或缺少項目時，「其他／新增」保留已填內容，補選或新增後返回表單，不提前寫入消費；一般情況不用逐步選擇。每張表單最多5欄、每個下拉最多25項。
- 維持操作者隔離、現金／未指定、儲存時啟用狀態檢查及撤銷規則。帳目修改可保留停用來源的歷史名稱；捷徑與最近再記須改選啟用來源。投資與既有文字指令保留。

### 指令、資料庫與升級

- 影響 `/記帳` 以及 `/生活`、`!help`、`!記帳說明` 進入的生活表單。使用既有 discord.py 2.7.1 元件，無新增套件、資料表或索引，SQLite交易策略不變。
- 從0.6.0／0.6.1升級：停止舊程序，重新執行 `python bot.py`，重新開啟新版面板，不沿用舊表單。更舊版本依 README 先備份資料庫，啟動時執行既有非破壞性升級。斜線指令可能需等待Discord同步，文字入口仍可備援。

### 驗證

- `tests/run_discord_validation.py`：76項測試通過，全部使用隔離資料庫。新增單張表單結構、Discord Label／Select提交解析、補選保留草稿、超過25項的預選，以及停用來源歷史保留／最近再記阻擋測試；既有生活與投資回歸通過。
- 未讀取Token、未修改正式 `data.db`、未啟動正式Bot。Discord傳送、同步與AI使用測試替身；實際手機／桌面表單顯示、安裝權限、斜線指令顯示時間及正式資料升級未驗證。

## 0.6.1 — 2026-09-10

### 修改

- 常用捷徑的新增、修改及使用改為先下拉選分類與付款來源，再開啟文字表單；不必手動輸入分類或來源名稱。新增／修改只填捷徑名稱、用途與選填金額；使用捷徑只填金額、日期與用途，確認儲存才新增消費。
- 沿用既有分類／付款來源選單、分頁與流程內新增功能；保留「現金／未指定」快速選擇。修改／使用時提示原分類及來源，啟用項目在首頁可標為原選項；停用來源不提供選取。
- 表單儲存仍重新驗證本人身分、分類與付款來源啟用狀態，選取後被停用亦不能寫入。捷徑管理及消費確認不改寫歷史紀錄；最近再記維持0.6.0原確認流程。

### 指令、升級與驗證

- 影響 `/生活`／`!help` 生活看板內的常用捷徑入口；原斜線指令、文字指令與投資功能保留。
- 無資料庫或套件變更。停止舊程序後重新 `python bot.py`，使用 `/生活` 或 `!help` 開啟新版面板；請重新開啟捷徑，不沿用舊表單。
- 71項隔離資料庫測試通過：原69項按新流程調整，加上捷徑新增／修改／使用全流程、空分類及流程內新增來源、所有者檢查、選取後停用來源阻擋的2項整合測試。確認選取不會提前新增消費，歷史資料及原生活／投資回歸通過。
- 未讀取Token、未修改正式 `data.db`、未啟動正式Bot；未登入Discord驗證實際手機／桌面下拉畫面。既有TextInput標籤棄用警告仍存在。

## 0.6.0 — 2026-09-10

### 新增與修改

- 新增Discord Application Commands：`/記帳` 直接開啟本人快速記支出流程（分類→付款來源→日期→金額用途）；`/生活` 直接開啟本人完整生活看板。沿用流程內新增分類／付款來源及月曆日期選擇，沒有新增訊息輸入框旁的自訂永久按鈕。
- Bot啟動時於 `setup_hook` 註冊生活Cog並同步斜線指令；同步成功／失敗使用不含內部例外或憑證的啟動日誌。同步失敗不阻止原文字指令使用。
- `!help` 主頁只保留「生活記帳／投資紀錄」兩個主要入口，移除獨立AI摘要按鈕。投資AI留在投資面板AI分頁，`!分析`／`!analyze` 仍可在私訊使用；`!help ai` 改為說明新位置。生活AI功能與限制未修改。
- 生活總覽／支出頁新增「常用捷徑」，可建立、查看、修改、上下排序與停用本人捷徑，管理清單含停用項目。保存名稱、分類、付款來源、用途與選填預設金額；超過25項可翻頁。
- 點捷徑只開啟預填確認表單，日期預設今天，可修改金額、分類、來源、日期及用途，確認儲存後才新增消費。分類與來源填啟用名稱；已停用來源會提示更換並阻擋儲存，確認前停用捷徑也會阻擋。付款來源改名保持來源身分，新消費保存儲存當時名稱，歷史紀錄不變。
- 「最近再記」依消費日期、編號倒序列出自己的最近6筆有效手動消費；排除已撤銷、固定／訂閱／分期及他人資料。選取後預填今日與原金額／分類／來源／用途，與捷徑共用確認及停用來源防護，不直接寫入或修改原紀錄。
- 「回顧」提供使用者主動開啟的本週／本月回顧，直接以程式統計，不呼叫AI、不增加背景通知或排程。本週比較週一至今天及上週相同星期範圍；本月顯示截至今日總額、主要分類、付款來源與預算，差額比較上月同日範圍。上月較短時，差額只比較雙方共同天數，本月總額仍截至今天，介面明列各日期。
- 任一比較期間無紀錄時不計差額；本月無紀錄但有預算時僅列額度，使用率標示資料不足，不推斷零消費。回顧保留「僅依已記錄資料統計」及私人提示，來源／預算內容超過8欄可翻頁。
- 新增 `lifestyle_ui.py` 集中捷徑、再記確認及回顧介面，重用既有私人選單／表單與記帳服務。沒有新增套件、外部服務或投資資料模型變更；未加入重複記帳提醒或其他明確排除功能。

### 資料庫、受影響指令與升級

- 新增 `spending_shortcuts` 與 `shortcuts_user_order` 索引，保存 `user_id`、名稱、分類、來源ID、用途、可空預設金額（整數分）、排序與啟用狀態；來源讀寫檢查使用者身分。沿用SQLite 10秒忙碌等待、立即交易與失敗回滾。
- 非破壞性升級可重複執行，原消費、來源、預算、固定項目、投資與歷史資料保留。停用／修改來源或捷徑不改寫歷史消費。`!生活清除 yes` 同步清除本人捷徑，投資仍保留。
- 新增 `/記帳`、`/生活`；調整 `!help`、`!help ai`；`!記帳說明`、原生活文字指令與批次處理、投資文字指令均保留。
- 升級前停止所有舊Bot程序，關閉資料庫連線並備份 `data.db`。使用原有套件環境執行 `python bot.py`，首次啟動自動建立捷徑資料表並同步斜線指令；重新開啟面板，不沿用舊表單。Discord端可能需要等待同步顯示；若未出現，確認應用程式指令權限／安裝範圍並查看安全啟動日誌，期間使用 `!help`／`!記帳說明` 備援。沒有新增額外設定或憑證需求。

### 實際驗證與未驗證事項

- 69項離線測試通過（原57項回歸依新主頁調整＋新12項），以暫存隔離資料庫驗證；測試入口禁止正式資料庫連線。涵蓋斜線註冊、同步成功／網路失敗安全日誌、私人流程與投資AI分頁、捷徑建立修改排序停用與所有者隔離、預填不寫入、來源／分類停用與已儲存表單防護、最近6筆篩選、週／月與短月比較、無資料預算提示、分頁、反覆升級、清除與歷史／投資保留。
- 執行 `python tests/run_discord_validation.py`；本機使用Codex附帶Python 3.12及既有discord.py 2.7.1套件，未安裝新依賴。斜線同步網路傳輸、Discord回覆與AI分析在測試中使用替身；測試訊息不表示實際上線同步。
- 獨立靜態審查指出無紀錄預算不可顯示0%使用率，已修正並加回歸測試。既有TextInput標籤棄用警告仍存在。
- 未登入Discord、未讀取Token、未修改正式 `data.db`、未啟動正式Bot；斜線指令實際可見時間、安裝權限、桌面／手機顯示、正式資料升級與真實AI服務尚未驗證。
- 本輪按小任務依序處理入口、資料／捷徑、回顧及驗證文件；AGENTS同步記錄後續採小任務執行的工作方式。

## 0.5.1 — 2026-09-10

### 修改與修正

- 「＋記支出」的日期改為同一張月曆卡片與完整日期下拉選單（YYYY-MM-DD及星期）；可原地切上月／下月／本月、選今天／昨天，不再先選月份再開另一個日期選單。未來日期不可選，支援閏年及跨月。Discord下拉選單最多25項，較長月份在原卡片翻頁。
- `!help` 的「💰 生活記帳」按下直接開啟本人生活看板，移除中間入口步驟；`!記帳說明` 的安全入口仍保留。
- 私訊中連續多筆生活寫入指令改為集中回覆，保留每筆序號、成功／失敗結果、原參數解析與各筆獨立交易；不再每寫一筆就等待一次Discord回覆。中途遇到投資或其他指令時結束分組，保留原執行順序，單筆指令行為不變，每則仍限50行。
- 支援集中回覆的指令：`!支出`、`!支出補登`、`!支出修改`、`!預算`、`!固定新增`、`!固定停用`、`!分類新增`、`!分類刪除`、`!提醒設定`、`!提醒重設`。公開頻道仍由原私訊限制阻擋，不集中處理。
- 批次結果送出後才發送預算提醒，沿用通知鎖防止背景通知插隊；傳送失敗時不把未送達提醒標為已送達。已成功寫入的帳目不因網路錯誤回滾，應先查看明細再重試失敗項目，不可整批重貼。

### 升級與驗證

- 無新資料庫結構或依賴，投資邏輯未變。停止舊程序後用有效Python環境重新執行 `python bot.py`，重新輸入 `!help` 取得新版按鈕。從更舊版本升級仍依0.5.0的備份／自動升級步驟。
- 57項離線測試通過（前版52項＋5項），使用隔離資料庫與真實discord.py解析／元件、模擬網路傳輸；包含完整日期、閏年、31天翻頁、本人隔離、直接看板入口、批次部分失敗、投資順序、公開頻道限制、回覆失敗保留資料與提醒、併發通知等待批次回覆，以及原有回歸。
- 隔離量測：50筆虛構支出連同原有固定項目同步及提醒查詢約0.245秒（不含Discord網路）；27筆混合有效／無效輸入測試的結果回覆少於6則。原流程逐筆等待網路回覆與使用者描述的停頓相符，這次減少傳送次數；未連線Discord重現實際限流或測量上線改善幅度。
- 未讀取Token、未修改正式 `data.db`、未啟動Bot；未實測Discord桌面／手機月曆顯示與真實網路負載。既有TextInput棄用警告仍存在。同步README與生活使用說明版本。

## 0.5.0 — 2026-09-10

### 新增與修改

- 第一階段生活消費記帳優化：付款來源、快速記支出、帳目管理與三種原生長條圖卡。投資指令及資料模型未修改，未增加依賴。
- 生活看板「付款來源」可建立、查看、改名及停用自己的來源；清單含停用項目，超過25筆可翻頁。「現金／未指定」為保留來源，不能改名或停用，隨時可快速記帳。
- 「＋記支出」依序選分類、付款來源、日期，再填金額與用途；可在流程新增分類或付款來源。日期提供今天／昨天捷徑，以及月份、日期下拉補登。同一表單儲存後不重複新增。
- 「帳目管理」按月顯示自己的有效消費，每頁6筆列出完整日期、金額、分類、用途及付款來源；選取單筆後開啟五欄表單。金額、分類、用途、日期與來源皆可修改；保留自動固定支出不可移動日期及最新操作撤銷規則。過期修改表單不覆蓋較新的帳目。
- 來源名稱以當筆快照保存，來源改名／停用不影響歷史。修改帳目時保留原值可沿用已停用的分類或來源；切換則須使用自己的啟用項目。撤銷會還原付款來源快照。
- 「支出圖表」提供選定月份（預設本月）的分類分布、付款來源分布，以及截至該月的最近六個月趨勢。歷史月計整月、本月截至今天；來源依歷史名稱分組。使用Discord原生長條、金額與百分比，超過8組可翻頁，無資料有空狀態；明示期間及「僅依已記錄資料統計」，不需圖片或外部圖表服務。
- 月報、預算、總覽、固定負擔與圖表僅統計未撤銷的消費；所有新介面與操作結果僅本人可見，檢查操作者與資料擁有者。
- 修正共用選單翻頁後附加按鈕消失，保留快速日期及帳目月份操作。

### 受影響指令與範圍

- `!help`／`!記帳說明` 的生活看板新增上述入口；`!支出`／`!支出補登` 保留原語法，省略來源記為「未指定」。`!支出修改` 原語法保留原付款來源，變更來源使用帳目管理；`!支出明細` 顯示歷史付款來源。
- `!月報`、`!預算`、`!記帳分析`、`!問` 沿用既有統計／查詢路徑；`!記帳撤銷` 兼容升級前的操作紀錄。`!生活清除 yes` 亦清除此使用者的付款來源，投資資料不受影響。
- 未加入餘額、轉帳、儲值、提款、信用卡帳單、帳單上傳、外部金融串接、匯率、跨幣別總計、通知內容擷取保存或AI自動入帳；既有固定項目及預算提醒保持運作。

### 資料庫與升級

- 新增 `payment_sources`，以 `user_id` 隔離，同使用者名稱唯一，含啟用狀態；新增使用者／啟用來源索引。
- `expenses` 以非破壞性升級新增 `payment_source_id`、`payment_source_name`、`kind`、`revision`，及使用者／消費類型／撤銷狀態／日期索引。舊支出預設為消費、付款來源名稱「未指定」；舊來源ID可為空，顯示與統計不受影響。升級可重複執行，不改寫既有金額、用途或投資持倉。
- SQLite連線忙碌等待設為10秒，沿用 `BEGIN IMMEDIATE` 寫入交易與失敗回滾；欄位升級亦使用交易。沒有改用其他資料庫。
- 升級前停止所有舊版程序，關閉資料庫連線後自行備份 `data.db`；使用已安裝 `requirements.txt` 的有效Python執行 `python bot.py`，首次啟動自動升級。重新輸入 `!help`／`!記帳說明` 開啟新面板，勿繼續操作舊表單。若需回復，停止程序後還原升級前程式與備份；不要讓舊版繼續寫入升級後資料庫。

### 實際驗證與未驗證事項

- 52項離線測試通過（原38項＋新14項），使用隔離暫存資料庫；包含真實discord.py元件及指令解析、兩種預設來源、流程內新增、來源改名停用、本人隔離、完整帳目翻頁、五欄修改、過期表單、撤銷快照、舊結構反覆升級、最近六個月（歷史月整月）、無資料圖表、非消費排除、固定項目規則，以及4個工作執行緒寫入24筆消費的完整性。既有投資回歸通過。
- 執行 `tests/run_discord_validation.py`；測試入口增加拒絕連線正式資料庫的防護，並可重用既有本地套件。此機器原 `.venv` 的Python啟動器失效，實測使用Codex附帶的Python 3.12與現有discord.py 2.7.1；未安裝新套件、未讀取Token、未操作正式 `data.db`。
- 獨立靜態審查未發現其他重大問題。既有測試仍有discord.py的TextInput標籤棄用警告，未影響通過。
- 未登入Discord實測桌面／手機、真實私訊權限及長時間併發負載；未對正式資料執行升級、未啟動Bot或測試真實Ollama。正式升級與上線畫面需依上述步驟驗證。

### 版本與文件整理

- 歷史對照：原0.3.0→0.2.3、0.3.1→0.2.4、0.4.0→0.2.5、0.5.0→0.3.0、0.6.0→0.3.1、0.7.0→0.3.2、0.8.0→0.4.0、0.9.0→0.4.1；0.1.0與0.2.0～0.2.2保留。本次完整功能擴充為0.5.0。
- 同步README、生活說明及AGENTS的版本與升級規則，保留原歷史記錄。

## 0.4.1 — 2026-09-09（原0.9.0）

### 新增與修改

- 生活看板新增支出、預算與固定項目的分類改用下拉選單，可直接新增分類；超過25項提供翻頁。
- 月份改用下拉選單；支出日期亦可選擇，固定項目提供本月／下月，預算可選未來12個月。金額、用途、名稱及期數保留逐格表單。
- 投資入口新增股票、基金、觀察、歷史、AI私人分頁：支援買賣、基金申贖與淨值、觀察管理、查價、交易歷史與撤銷表單，沿用原交易處理；不影響文字指令語法。
- 預算頁新增提醒設定按鈕，提供預設80%／100%、常用組合、百分比多選及自訂表單。
- 帳務文字指令限私訊；公開頻道保留help、guide、privacy、ping、price、fund、記帳說明。伺服器透過help開啟私人面板，選單與表單檢查操作者身分。
- 本地AI網址限本機回送位址、禁止HTTP重新導向及明確cloud模型名稱；錯誤回覆不顯示內部例外細節。隱私說明補充Discord仍接收輸入、資料庫未加密及主機管理者可讀取的界線。

### 升級與驗證

- 無資料庫結構變更。停止舊版後執行 `python bot.py`，重新輸入 `!help` 開啟新版面板；帳務文字指令改在私訊使用。面板閒置5分鐘後需重新開啟。
- 38項離線測試通過，包含選單分頁、月份選項、表單欄位、投資寫入、操作者隔離、私訊限制、本機網址限制及既有記帳回歸。測試使用隔離資料庫，未修改正式data.db、未讀取Token。
- 未登入Discord實測桌面／手機畫面，未測試真實Ollama連線；未導入資料庫加密或修改作業系統檔案權限。本機Ollama服務仍需使用下載的本地模型。

## 0.4.0 — 2026-09-09（原0.8.0）

### 新增與修改

- 套用使用者選定的混合介面：分頁導航、簡潔Embed卡片、預算進度條。
- 生活看板分為總覽、支出、預算、固定負擔、AI，切換與翻頁更新原卡片，僅本人可見並檢查操作者身分。
- 支出每頁6筆，預算與固定設定每頁5筆；顯示頁碼、空資料提示與上一／下一頁按鈕。
- 新增支出、預算、切換月份與自然語言查詢表單；支出／預算表單成功後更新看板。
- 預算顯示實際使用率百分比、進度條、已用與剩餘／超支；超過100%不截斷百分比。
- 保留固定項目新增表單、月／週AI分析及全部文字指令；AI頁明示分析以今天為基準。
- 新增 dashboard.py；!記帳說明 改成簡短入口卡片，詳細指令移至說明按鈕。

### 升級與驗證

- 無資料庫變更。停止舊版後重新 python bot.py，再輸入 !記帳說明 開啟新看板。看板閒置5分鐘後需重新開啟。
- 36項離線測試通過，含五分頁卡片、Discord長度限制、翻頁、進度條飽和、私人回覆、操作者隔離、表單写入與原功能回歸。
- 未登入Discord實測畫面，未讀取Token或修改正式data.db；等待使用者確認實際手機／桌面顯示。

## 0.3.2 — 2026-09-07（原0.7.0）

### 新增與修改

- !提醒設定 可查看或自訂預算提醒百分比，未設定維持80%／100%；!提醒重設 恢復預設。門檻適用同使用者所有總額與分類預算，修改後檢查本月並取消未送出的停用門檻通知。
- !分類清單、!分類新增、!分類刪除：每人可管理分類；刪除採停用方式，保留歷史、預算與既有固定支出，新增同名可恢復。
- 記帳、預算、表單及AI分類建議使用啟用分類；月報、趨勢與自然語言歷史查詢保留停用分類。
- 預算報表、查詢與提醒明列百分比「使用率」，與分類支出「占比」分開。
- 介面整體改版尚未套用，等待使用者選擇樣式。

### 升級與驗證

- 首次啟動新增 spending_categories、spending_settings；保留既有帳目，!生活清除 同步清除此使用者的自訂設定。
- 停止後重新啟動 python bot.py，重新輸入 !記帳說明。
- 36項離線測試通過，包含自訂分類、停用／恢復、跨使用者隔離、門檻去重與恢復預設。未登入Discord、未更動正式資料庫。

## 0.3.1 — 2026-09-07（原0.6.0）

### 新增與修改

- 生活選單新增固定支出／訂閱／分期表單：先選種類，再填有標籤的名稱、金額、分類與月份；只有分期需要總期數，不必理解或輸入0。
- 文字指令保留；說明改成欄位名稱加範例，明確說明0表示持續至停用，訂閱／固定可以省略期數。
- 投資及生活AI摘要改成最多三個短重點，每點最多60字，以空行分段；統計限制改為一次簡短註記。
- 自然語言單月支出／分類查詢由程式直接呈現金額、占比及分類預算，不再追加冗長AI分析，避免月份誤說成當週。
- 金額共用精簡格式：150.00→150、1234.50→1,234.5；生活月報、明細、固定負擔、提醒及投資估值等一併套用，不改變儲存或計算。
- 空白、非JSON或未完成的模型回覆改為「AI 回覆未完成，請再試一次」，不顯示 Expecting value 等解析錯誤；支援JSON程式碼區塊。

### 升級與驗證

- 無資料庫變更。停止舊程序後重啟，重新輸入 !help 或 !記帳說明取得新表單。
- 34項離線測試通過，涵蓋格式、短摘要、錯誤提示、表單欄位及寫入、單月查詢期間與統一註記；正式資料庫與Token未讀寫。
- Ollama qwen3:8b 使用虛構資料實測短分段摘要，未登入Discord；表單實際畫面待使用者上線確認。

## 0.3.0 — 2026-09-07（原0.5.0）

### 新增

- 獨立的台幣生活記帳：!支出、!支出補登、!支出明細、!支出修改、!記帳撤銷。金額使用整數分保存，含分類、用途、日期及撤銷紀錄；不加入收入、銀行餘額或投資資金連動。
- !預算 設定月份總額及分類預算，分類合計不可超過總額；!月報 顯示分類占比、固定負擔、已用及剩餘預算。
- 預算80%／100%門檻提醒，同使用者同月份同分類同門檻只記一次；預算逐月設定，不自動沿用。
- !固定新增、!固定清單、!固定停用：支援固定支出、訂閱及有限期分期。月初自動列支、下次啟動補記缺失月份、同項目同月份去重、分期結束停止。付款日可選填備註，分期金額為每期金額。
- 自動記帳與背景預算通知以私訊傳送，送達失敗時保留至下一次生活指令顯示；運行中每分鐘檢查。
- !記帳分析 月/週 期數：支援2～12期同天數比較，程式先算金額、分類占比及百分點變化，區分固定與日常支出。AI 說明資料不足、月初集中記帳及預算調整選項，不自動改預算。
- !分類建議：依用途取得 AI 分類建議，不直接記帳，使用者可選擇或更正。
- !問／!ask：自然語言唯讀查詢單月生活報表、近期趨勢、本月固定負擔、投資持倉及最近20筆投資交易。模型只能輸出驗證後的限定查詢計畫，不能指定其他使用者或執行任意 SQL／修改資料。
- !生活清除 yes 只刪除生活資料；原 !clear yes 仍只清除投資資料。

### 修改與檔案

- !help 主頁整理為投資紀錄、生活記帳兩個入口，保留投資 AI 摘要按鈕。生活選單提供本月支出、固定負擔與記帳 AI 分析按鈕，結果僅點擊者可見。
- 新增 spending.py（資料與計算）、spending_commands.py（指令與排程）、query_plan.py（查詢驗證）、SPENDING_GUIDE.md（完整操作與測試資料）。
- ai.py 共用本地聊天呼叫並支援結構化回覆；更新隱私及清除範圍說明。
- 移除啟動輸出的 emoji，避免部分 Windows 終端編碼造成啟動失敗。

### 資料與升級

- 首次啟動新增 spending_users、expenses、expense_actions、budgets、recurring_expenses、spending_notices 表及索引；不改動投資資料。
- 停止舊程序後重新執行 python bot.py，重新輸入 !help。原 Token 設定維持，使用既有 discord.py、aiohttp 與 Ollama qwen3:8b。
- 月初自動記帳不是實際銀行扣款；未記錄不代表沒有消費。固定項目本月開始立即補記，下月開始等待下一月；停用保留已記帳紀錄。
- 多行重新貼上會再次新增支出／固定設定；只重試失敗行。固定排程的月份去重不等於使用者手動重複建立項目去重。
- 預算與AI摘要不含收入；生活與投資不合併成總資產。自然語言暫不支援寫入、任意日期範圍或歷史投資快照。

### 驗證

- 本版31項離線測試全部通過，包含實際discord.py指令與按鈕整合測試。
- 使用臨時資料庫驗證金額精度、編修與撤銷、使用者隔離、預算門檻去重、分期補記／結束、停用及撤銷後不重建、月份短月對齊、分類占比與金額差異、生活清除不影響投資。
- 使用實際 discord.py 離線驗證指令參數解析、選單建立、私人按鈕回覆、自然語言唯讀路徑與背景通知去重。未登入 Discord、未讀取 Token、未改動正式 data.db。
- Ollama qwen3:8b 實際使用三個假問題測試單月分類支出、近四週比較及拒絕刪除操作，查詢分類通過。
- 尚需使用者在 Discord 上線後確認實際顯示、私訊權限與記帳AI分析品質；AI 摘要品質不由程式測試保證。

## 0.2.5 — 2026-09-07（原0.4.0）

### 新增與修改

- !help 同一選單新增「🤖 AI 摘要」按鈕，點擊直接執行持倉摘要；先確認互動再進行行情及模型請求。
- 按鈕使用點擊者自己的身分取得資料，結果僅點擊者可見；不會使用選單發送者的投資資料。
- 保留 !分析／!analyze，新增 !help ai 說明。按鈕與文字指令共用分析流程及使用者併發限制，避免同時重複分析。
- 無資料庫變更。重新啟動後重新輸入 !help 取得新版按鈕；沿用選單 120 秒有效期。

### 驗證

- 離線驗證互動延後回覆、點擊者身分、私人回覆、重複分析阻擋與失敗後限制釋放；未登入 Discord 實測。

## 0.2.4 — 2026-09-07（原0.3.1）

### 修改

- 同一訊息連續的 !watch 合併執行與回覆，只列一次新增標的及已存在標的；也支援 !watch 2330 AAPL，重複代號去重。
- 遇到其他指令時不跨越合併，保留操作順序。
- 每個指令第一則回覆增加分隔線與空行；支援文字及 Embed，後續分段不重複加線，避免另外發送分隔訊息。
- README 更新操作說明；無資料庫結構變更，重新啟動後套用。

### 驗證

- 離線測試確認合併觀察僅一則摘要、已存在標的、跨指令順序與每次回覆分隔行為；尚未在 Discord 實測此版。

## 0.2.3 — 2026-09-07（原0.3.0）

### 新增與修正

- 支援同一則 Discord 訊息貼上多行完整指令，按順序逐行執行；適用觀察清單、股票買賣、基金記帳及其他現有指令。保留 Discord 原生參數解析，每則最多 50 行。
- 可辨識外層程式碼區塊並忽略空白行，避免原本只執行第一行而漏掉後續資料。機器人訊息不處理。
- 觀察清單查價失敗時保留股票代號並顯示提示，不再靜默隱藏。
- history 與基金淨值更新時間改為台灣時間 YYYY-MM-DD HH:MM:SS，不顯示小數秒；資料庫 UTC 儲存不變。
- 未知指令顯示錯誤提示。

### 使用與驗證

- 重新啟動套用。每行獨立執行，錯誤不回滾已成功項目；勿為重試其中一筆而重貼整批。
- 隔離資料庫測試實際指令回呼：兩檔 watch、兩筆買入的平均成本 950／20 股、兩筆基金、保存淨值，以及 AAPL 查價失敗仍顯示。
- 測試程式碼區塊、空白行、批次上限、機器人訊息忽略與台灣時間跨日。尚未在 Discord 實測本版，未改動正式資料庫。

## 0.2.2 — 2026-09-07

### 修正

- 發現磁碟上的 bot.py 啟動區塊仍使用舊的環境變數檢查，造成已有 token.txt 時仍提示設定 DISCORD_TOKEN。
- 恢復啟動區塊呼叫 config.load_token，保留檔案讀取與登入失敗提示。
- 新增啟動接線回歸測試，實際執行主程式區塊並以假 Bot 驗證 Token 傳遞；未連線 Discord、未修改資料庫。

## 0.2.1 — 2026-09-07

### 修正與新增

- 啟動恢復使用設定讀取，移除 bot.run 內直接填入的字串。
- 新增 config.py 與本機 token.txt：優先使用 DISCORD_TOKEN，沒有設定時讀取專案內 token.txt，關閉終端後仍可重新啟動。
- 缺少 Token、格式錯誤與 Discord 拒絕登入時提供明確處理提示，不顯示憑證內容。
- token.txt 加入 .gitignore；更新 README 的設定與啟動方式。

### 升級與驗證

- 請自行將完整 Token 存入 token.txt；不再貼入 bot.py。若舊環境變數仍有失效 Token，先移除該變數。
- 無資料庫變更。使用假憑證驗證檔案讀取、環境變數優先順序及缺值／格式錯誤；未登入 Discord。

## 0.2.0 — 2026-09-07

基金淨值保存、交易歷史與撤銷。

### 新增

- `!fundprice 名稱 淨值`：保存目前使用者的基金淨值及 UTC 更新時間；驗證淨值為有效正數及基金名稱存在。
- `!history`：查看最近 20 筆交易；`!history 數量` 可指定 1～100 筆，顯示交易編號、時間、類型、價格、數量、賣出損益與撤銷狀態。
- `!undo`：預覽最新有效交易；`!undo 編號` 確認撤銷。支援股票買入、賣出、移除與基金申購、贖回，可按順序撤銷。
- `ledger.py`：集中處理交易、歷史與撤銷；持倉變更與歷史寫入在同一資料庫交易中完成，失敗時回滾。
- 新增基金淨值表 `fund_prices`、交易歷史表 `trade_history`。

### 修改

- `!fundcheck`、`!portfolio`、`!分析` 共用已保存淨值，不再每次等待使用者輸入；顯示手動淨值的更新時間，缺價時提示使用 `!fundprice`。
- 股票與基金買賣、股票移除改由共用交易模組處理，回覆交易編號及剩餘持倉。
- 撤銷保留歷史並標記已撤銷；檢查使用者與最新有效交易編號，阻止跨使用者、過期確認與重複撤銷。
- `!clear yes` 同時清除目前使用者的淨值與交易歷史。
- 更新功能選單、README 與測試。

### 資料與升級

- 重新啟動時自動新增資料表，保留原持倉與基金交易。部署前停止舊程序，避免舊程式繼續寫入未記錄的交易。
- 歷史從此版本開始記錄；更新前的買賣歷史不回填，交易時間為實際記帳時間。
- 基金買賣價格不自動覆寫保存的淨值；撤銷交易也不撤銷淨值更新。
- `!clear yes` 清除後無法透過 `!undo` 恢復。

### 驗證

- 11 項離線測試通過，涵蓋加權成本、賣出與連續撤銷、基金全部贖回還原、淨值保存與讀取、使用者隔離、舊持倉保留及無效輸入。
- Python 語法檢查通過。此版本尚未收到 Discord 端到端操作驗證結果。
- 開發測試使用隔離資料庫，未修改現有 `data.db`。

### 文件補充

- 補建本更新紀錄，回溯整理 0.1.0 與 0.2.0。
- 新增 `AGENTS.md`，要求後續修改同步記錄版本、內容、資料影響與驗證結果；此次文件整理不另增加程式版本。

## 0.1.0 — 2026-09-07

修正原有功能、移除停用指數，加入 Ollama 本地 AI 摘要。

### 新增

- `!分析`／`!analyze`：取得目前使用者的持倉估值後，呼叫本地 Ollama 產生繁體中文摘要。
- `ai.py`：預設使用 `qwen3:8b`、`http://127.0.0.1:11434`，可透過 `OLLAMA_MODEL`、`OLLAMA_URL` 設定；加入逾時、併發限制及失敗提示，AI 不可用時保留記帳與查價。
- `portfolio.py`：共用股票代號正規化、數字驗證及持倉估值。
- `requirements.txt`、`.gitignore`、README 與離線測試。

### 修正與修改

- 修正 `!check` 讀取三個欄位卻只接兩個值的錯誤，改為顯示平均成本、數量、成本、市值、整體損益與報酬率。
- 統一股票代號處理，修正 `.TWO` 後綴被錯誤截除的問題。
- 股票與基金買賣驗證有效正數，加入指令參數錯誤提示。
- `!check`、`!portfolio`、AI 共用估值結果；不同幣別分開計算，未設定幣別的基金逐檔估值。
- 缺少股票行情或有效基金淨值時標記估值不完整，不將基金缺價當作零。
- 此版本基金查詢仍採手動回覆淨值，加入逾時與頻道檢查；0.2.0 改為保存淨值。
- 同步行情請求移至背景執行緒，加入 30 秒快取與長訊息分段。
- 資料庫路徑固定為專案內 `data.db`，避免啟動目錄不同而讀到另一份資料。
- 啟動改由 `DISCORD_TOKEN` 環境變數取得 Token，加入主程式進入點，匯入模組不自動登入。

### 移除

- 移除已停用的 `!index`、`!indexlist`、`!indexadd`、`!indexremove`。
- 移除 `INDEX_MAP`、`get_index()` 及相關匯入，不再建立或操作 `indexes` 表。
- 移除程式內硬編碼的 Bot Token。

### 資料與升級

- 保留現有資料；舊的 `indexes` 表不做破壞性刪除，但不再使用。
- 需使用有效 Python 環境安裝 requirements，於啟動終端設定 `DISCORD_TOKEN`。本地摘要需 Ollama 執行並已下載模型。
- 報酬仍為目前持倉的未實現損益，不含跨幣別換算、股息、手續費與稅金。

### 驗證

- 5 項離線測試及語法檢查通過，涵蓋代號、數值、損益、幣別與缺漏行情及使用者隔離。
- 本地 Ollama 可找到 `qwen3:8b`，聊天 API 實際呼叫成功。
- 後續使用者成功登入 Discord，並回報測試記帳、估值與本地 AI 流程均成功。
